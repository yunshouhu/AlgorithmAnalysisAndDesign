-- define target
target("bogo_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("bogo_sort_test.c")

-- define target
target("bubble_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("bubble_sort_test.c")

-- define target
target("bucket_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("bucket_sort_test.c")

-- define target
target("cocktail_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("cocktail_sort_test.c")

-- define target
target("comb_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("comb_sort_test.c")

-- define target
target("counting_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("counting_sort_test.c")

-- define target
target("gnome_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("gnome_sort_test.c")

-- define target
target("heap_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("heap_sort_test.c")

-- define target
target("insertion_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("insertion_sort_test.c")

-- define target
target("merge_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("merge_sort_test.c")

-- define target
target("odd_even_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("odd_even_sort_test.c")

-- define target
target("pancake_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("pancake_sort_test.c")

-- define target
target("quick_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("quick_sort_test.c")

-- define target
target("radix_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("radix_sort_test.c")

-- define target
target("selection_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("selection_sort_test.c")

-- define target
target("shell_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("shell_sort_test.c")

-- define target
target("tree_sort_test")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tree_sort_test.c")

