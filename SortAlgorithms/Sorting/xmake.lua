-- define target
target("BubbleSorting")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("BubbleSorting.cpp")

-- define target
target("InsertionSorting")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("InsertionSorting.cpp")

-- define target
target("QuickSorting")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("QuickSorting.cpp")

-- define target
target("SelectionSorting")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("SelectionSorting.cpp")

-- define target
target("ShellSorting")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("ShellSorting.cpp")

-- define target
target("StupidSorting")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("StupidSorting.cpp")

